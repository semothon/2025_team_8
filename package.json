{
  "name": "semoton",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "bun run --filter ... dev",
    "build": "bun run --filter ... build",
    "lint": "bun run --filter ... lint"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}